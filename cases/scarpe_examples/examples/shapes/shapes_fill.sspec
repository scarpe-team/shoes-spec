---
----------- app code
Shoes.app do
    shape do
        fill "blue"
        stroke "green"
        line 0,0, 100, 100

        fill "red"
        stroke "black"
        star 23, 0, 6, 50, 25
    end
end

----------- test code
html = dom_html

# Should render shape with colors
# Colors might be in various formats
assert html.include?("svg") || html.include?("canvas") || html.include?("path"),
       "Expected shape elements in DOM"
