---
----------- app code
Shoes.app do
  star 230, 100, 6, 50, 25
end

----------- test code
# Star should render as SVG
html = dom_html

# Should contain SVG elements for the star
assert html.include?("svg") || html.include?("polygon") || html.include?("path"), 
       "Expected SVG/polygon/path elements for star"
