---
----------- app code
Shoes.app do
  shape do
    move_to(90, 55)
    line_to(400,10)
    line_to(400,90)
  end
end

----------- test code
# Shape with line_to should render
html = dom_html

# Should contain SVG/canvas elements
assert html.include?("svg") || html.include?("path") || html.include?("canvas"),
       "Expected SVG or canvas elements for shape"
