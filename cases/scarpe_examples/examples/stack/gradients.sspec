---
----------- app code
Shoes.app do
  stack height: 200, width: 200 do
    border "#090979".."#ac033d", strokewidth: 15
    background "#ac033d".."#090979"
  end
end

----------- test code
html = dom_html

# Stack should render (even if empty of text content)
# Check for gradient-related styling or the colors
assert html.include?("200") || html.include?("gradient") || html.include?("090979") || html.include?("ac033d"), 
       "Expected gradient or dimension styling"
