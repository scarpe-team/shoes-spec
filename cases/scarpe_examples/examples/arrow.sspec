---
----------- app code
Shoes.app do
  arrow 100, 100, 30
  arrow 150, 120, 40
  arrow 30, 60, 40
  arrow :left => 50, :top => 80, :width => 40
end

----------- test code
html = dom_html

# Arrows should render as SVG/polygon elements
assert html.include?("svg") || html.include?("polygon") || html.include?("path"),
       "Expected SVG arrow elements"
