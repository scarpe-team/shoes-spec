---
----------- app code
Shoes.app do
  stack do
    @note = para " your note will appear here"

    @edit_box = edit_box ""
    @save_button = button "Save"

    @save_button.click do
      new_text = @edit_box.text
      @note.text = new_text
      alert("Note saved successfully!")
    end
  end
end

----------- test code
# Get the para with initial text
note = para("@note")
assert_equal " your note will appear here", note.text

# Get the edit box and button
eb = edit_box()
btn = button("@save_button")

# Type some text
eb.text = "Hello from test!"

# Click save (this will trigger alert, but we can still test the para update)
btn.trigger_click

# Para should now have the new text
assert_equal "Hello from test!", note.text
