---
# Auto-imported from scarpe/legacy/for_playtest/shoes-contrib/basic/scribble.rb
# TODO: Add meaningful assertions
----------- app code
Shoes.app do
  ox, oy = nil, nil
  animate 24 do
    b, x, y = self.mouse
    line(ox, oy, x, y)  if b == 1
    ox, oy = x, y
  end
end

----------- test code
# Scribble uses animate and mouse tracking to draw lines
# Animation-based, hard to assert behavior; verify app starts
assert true, "Scribble app with animate and mouse tracking initializes"
