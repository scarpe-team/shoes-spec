---
# Auto-imported from scarpe/legacy/for_playtest/shoes-contrib/basic/basic-new-window.rb
# TODO: Add meaningful assertions
----------- app code
Shoes.app :title => "MAIN" do
  para self
  button "Spawn" do
    window :title => "CHILD" do
      para self
    end
  end
end

----------- test code
# Verify main window has spawn button
assert_equal "Spawn", button().text, "Should have Spawn button"

# Verify para exists (shows self reference)
assert para(), "Should have a para element"
