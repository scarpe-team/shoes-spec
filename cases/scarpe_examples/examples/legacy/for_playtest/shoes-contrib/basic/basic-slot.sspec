---
# Auto-imported from scarpe/legacy/for_playtest/shoes-contrib/basic/basic-slot.rb
# TODO: Add meaningful assertions
----------- app code
Shoes.app do
  stack :width => 200, :height => 200, :scroll => true do
    background "#2F2F2F"
    60.times do |i|
      para "Paragraph No. #{i}"
    end
  end
end

----------- test code
# Verify 60 paragraphs are created
paras = para(:all)
assert_equal 60, paras.count, "Should have exactly 60 paragraphs"

# Verify paragraph content pattern
assert_equal "Paragraph No. 0", paras[0].text
assert_equal "Paragraph No. 59", paras[59].text
