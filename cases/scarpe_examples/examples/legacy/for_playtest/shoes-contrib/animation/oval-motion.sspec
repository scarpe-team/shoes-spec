---
# Auto-imported from scarpe/legacy/for_playtest/shoes-contrib/animation/oval-motion.rb
# TODO: Add meaningful assertions
----------- app code
Shoes.app do
  @o = oval :top => 0, :left => 0, :radius => 40
  
  stack :margin => 40 do
    title "Dancing With a Circle"
    subtitle "How graceful and round."
  end

  motion do |x,y|
    @o.move width - x, height - y
  end
end

----------- test code
# Verify title and subtitle content
assert_equal "Dancing With a Circle", title().text
assert_equal "How graceful and round.", subtitle().text

# Verify oval exists
assert oval(), "Should have an oval element"
