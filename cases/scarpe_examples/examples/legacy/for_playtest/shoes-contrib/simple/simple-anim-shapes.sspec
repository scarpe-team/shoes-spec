---
# Auto-imported from scarpe/legacy/for_playtest/shoes-contrib/simple/simple-anim-shapes.rb
# TODO: Add meaningful assertions
----------- app code
Shoes.app do
  background rgb(0, 0, 0)
  fill rgb(255, 255, 255)
  rects = [
    rect(0, 0, 50, 50),
    rect(0, 0, 100, 100),
    rect(0, 0, 75, 75)
  ] 
  animate(24) do |i|
    rects.each do |r|
      r.move((0..400).rand, (0..400).rand)
    end
  end
  button "OK", :top => 0.5, :left => 0.5 do
    quit unless confirm "You ARE sure you're OK??"
  end
end

----------- test code
# Tests animated shapes with random movement
# Creates 3 rectangles that move randomly at 24fps
# Button triggers confirm() dialog then quit()
#
# The app uses:
# - background, fill, rect
# - animate(24) for continuous movement
# - confirm() dialog (requires mocking)
# - quit() (terminates app)

# Verify button exists
assert_equal "OK", button().text

# DO NOT trigger button - it calls confirm() then quit()
# Animation testing requires frame simulation
