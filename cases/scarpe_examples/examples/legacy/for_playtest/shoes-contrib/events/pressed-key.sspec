---
# Auto-imported from scarpe/legacy/for_playtest/shoes-contrib/events/pressed-key.rb
# TODO: Add meaningful assertions
----------- app code
Shoes.app do
  @info = para "NO KEY is PRESSED."
  keypress do |k|
    @info.replace "#{k.inspect} was PRESSED."
  end
end

----------- test code
# Keypress event handler - shows which key was pressed
# Verify initial state
p = para()
assert p.text.include?("NO KEY"), "Initial state shows 'NO KEY is PRESSED'"
