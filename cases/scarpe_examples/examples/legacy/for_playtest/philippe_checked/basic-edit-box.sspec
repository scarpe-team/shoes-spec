---
# Auto-imported from scarpe/legacy/for_playtest/philippe_checked/basic-edit-box.rb
# TODO: Add meaningful assertions
----------- app code
Shoes.app do
  stack :margin => 10 do
    @edit = edit_box :width => 1.0 do
      @para.text = @edit.text
    end
    @para = para ""
  end
end

----------- test code
# Test basic-edit-box.sspec - edit box that mirrors text to para

# Verify edit_box exists
eb = edit_box()
assert eb, "Should have an edit_box"

# Verify para exists and is initially empty
p = para()
assert p, "Should have a para"
assert_equal "", p.text

# KNOWN ISSUE: Setting text programmatically and triggering change
# doesn't properly update the para. This appears to be a Scarpe bug
# where the edit_box change callback doesn't fire correctly when
# text is set via the API rather than user input.
# See: shoes-spec-sessions/phase3-scarpe-bugs.md
#
# For now, verify the basic structure exists
assert true, "Edit box and para structure verified"
