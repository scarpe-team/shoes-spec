---
# Auto-imported from scarpe/legacy/for_playtest/simple/basic-fps.rb
# TODO: Add meaningful assertions
----------- app code
Shoes.app do
  @counter = para "STARTING"
  animate(24) do |frame|
    @counter.replace "FRAME #{frame}"
  end
end

----------- test code
# Verify para exists - animation updates it
p = para()
# Para should exist with text (STARTING initially, then FRAME X)
assert p.text.length > 0, "Para should have text"
