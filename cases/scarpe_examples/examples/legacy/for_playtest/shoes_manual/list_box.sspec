---
# Auto-imported from scarpe/legacy/for_playtest/shoes_manual/list_box.rb
# TODO: Add meaningful assertions
----------- app code
#!ruby
 Shoes.app do
   para "Choose a fruit:"
   list_box items: ["Grapes", "Pears", "Apricots"],
     width: 120, choose: "Apricots" do |list|
       @fruit.text = list.text
   end

   @fruit = para "No fruit selected"
 end

----------- test code
# Verify list_box with items and default selection
assert paras().length >= 2, "Expected 2 para elements"
assert_includes paras()[0].text, "Choose a fruit"
assert list_boxes().length >= 1, "Expected a list_box element"
# Default selection is Apricots
assert_equal "Apricots", list_box().text
