---
# Auto-imported from scarpe/legacy/for_playtest/shoes_manual/image.rb
# TODO: Add meaningful assertions
----------- app code
#!ruby
 Shoes.app do
   para "Nice, nice, very nice.  Busy, busy, busy."
   image "#{DIR}/docs/static/avatar.png"
  end

----------- test code
# Verify para text and local image element exist
assert paras().length >= 1, "Expected a para element"
assert_includes para().text, "Nice, nice, very nice"
assert images().length >= 1, "Expected an image element"
