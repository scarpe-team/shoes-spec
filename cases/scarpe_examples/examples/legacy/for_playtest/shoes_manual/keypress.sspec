---
# Auto-imported from scarpe/legacy/for_playtest/shoes_manual/keypress.rb
# TODO: Add meaningful assertions
----------- app code
#!ruby
 Shoes.app do
   @info = para "NO KEY is PRESSED."
   keypress do |k|
     @info.replace "#{k.inspect} was PRESSED."
   end
 end

----------- test code
# Verify keypress handler is set up with initial state
assert paras().length >= 1, "Expected a para element"
assert_equal "NO KEY is PRESSED.", para().text
