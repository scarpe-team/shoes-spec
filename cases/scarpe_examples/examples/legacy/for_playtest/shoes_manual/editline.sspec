---
# Auto-imported from scarpe/legacy/for_playtest/shoes_manual/editline.rb
# TODO: Add meaningful assertions
----------- app code
#!ruby
 Shoes.app do
   stack do
     edit_line
     edit_line width: 400
   end
 end

----------- test code
# Verify edit_line elements are created in stack
lines = edit_lines()
assert lines.length >= 2, "Expected at least 2 edit_line elements"
# Second edit_line should have width 400
assert stacks().length >= 1, "Expected a stack container"
