---
# Auto-imported from scarpe/legacy/for_playtest/shoes_manual/progress_bar.rb
# TODO: Add meaningful assertions
----------- app code
Shoes.app do
   stack margin: 0.1 do
     title "Progress example"
     @p = progress width: 1.0

     animate do |i|
       @p.fraction = (i % 100) / 100.0
     end
   end
 end

----------- test code
# Verify progress bar with animated fraction
assert stacks().length >= 1, "Expected a stack container"
assert titles().length >= 1, "Expected a title element"
assert_includes title().text, "Progress"
assert progresses().length >= 1, "Expected a progress bar element"
