---
# Auto-imported from scarpe/legacy/for_playtest/shoes_manual/mouse.rb
# TODO: Add meaningful assertions
----------- app code
#!ruby
 Shoes.app do
   @p = para
   animate do
     button, left, top = self.mouse
     @p.replace "mouse: #{button}, #{left}, #{top}"
   end
 end

----------- test code
# Verify mouse tracking animation updates para
assert paras().length >= 1, "Expected a para element for mouse display"
