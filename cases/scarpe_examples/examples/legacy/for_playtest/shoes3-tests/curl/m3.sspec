---
# Auto-imported from scarpe/legacy/for_playtest/shoes3-tests/curl/m3.rb
# TODO: Add meaningful assertions
----------- app code
Shoes.app do
   stack do
     title "GET Google", :size => 16
     @status = para "One moment..."

     download "http://www.google.com/search?q=shoes", 
         :method => "GET" do |dump|
       @status.text = dump.response.body
     end
   end
 end

----------- test code
# Tests download with :method => "GET" - requires network
skip "Network download tests require external URL availability"
