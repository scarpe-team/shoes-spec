---
# Auto-imported from scarpe/legacy/working/shoes_manual/utf8_support.rb
# TODO: Add meaningful assertions
----------- app code
Shoes.app do
 stack margin: 10 do
   @edit = edit_box width: 1.0 do
     @para.text = @edit.text
   end
   @para = para ""
 end
end

----------- test code
# Verify edit_box exists via instance variable
eb = drawable(Shoes::EditBox, "@edit")
refute_nil eb

# Verify para exists (initially empty)
p = drawable(Shoes::Para, "@para")
assert_equal "", p.text
