---
# Auto-imported from scarpe/legacy/working/shoes-contrib/kernel/confirm.rb
# TODO: Add meaningful assertions
----------- app code
if confirm("Draw a circle?")
  Shoes.app{ oval :top => 0, :left => 0, :radius => 50 }
end

----------- test code
# confirm() triggers a native modal dialog before app even starts
# Cannot automate without mocking the dialog system
skip "confirm() shows native dialog before app - cannot automate"
