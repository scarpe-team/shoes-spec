---
----------- app code
Shoes.app :width => 320, :height => 420 do
  stack :margin => 40 do
    stack :margin => 10 do
      para "Name"
      @name = list_box :items => ["Phyllis", "Ronald", "Wyatt"]
    end
  end
end

----------- test code
html = dom_html

# Label should render
assert html.include?("Name"), "Expected Name label"

# List box items should be in DOM (as options or similar)
assert html.include?("Phyllis") || html.include?("select"), "Expected list box with Phyllis option"
