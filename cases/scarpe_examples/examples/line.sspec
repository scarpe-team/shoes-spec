---
----------- app code
Shoes.app do
    line 0, 0, 100, 100
    line 100, 10, 0, 100
    line 50, 9, 50, 100
end

----------- test code
html = dom_html

# SVG line elements should be present
assert html.include?("svg") || html.include?("line") || html.include?("path"),
       "Expected SVG line elements"
