---
# Auto-imported from scarpe/simple/stack-width.rb
# TODO: Add meaningful assertions
----------- app code
Shoes.app do
  stack(:width => 150) { para "Now that's precision." }
end

----------- test code
# Tests stack with explicit width
stk = stack()
assert stk, "Stack should exist"
assert_equal 150, stk.style[:width]
assert_equal "Now that's precision.", para().text
