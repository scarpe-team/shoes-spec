---
# Auto-imported from scarpe/shoes_manual/animate.rb
# TODO: Add meaningful assertions
----------- app code
#!ruby
 Shoes.app do
   @counter = para "STARTING"
   animate(24) do |frame|
     @counter.replace "FRAME #{frame}"
   end
 end

----------- test code
# Verify initial state (animation will change it)
# Animation runs at 24fps so first frame happens quickly
assert true, "Animation app should render"
