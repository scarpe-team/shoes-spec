---
# Auto-imported from scarpe/simple/clear-slot.rb
# TODO: Add meaningful assertions
----------- app code
Shoes.app do
  @slot = stack { para "Old text" }
  button "Update" do
    @slot.clear { para "brand new text" }
  end
end

----------- test code
# Test: Slot clear and replace functionality
# Button clears stack and adds new content

assert_equal "Old text", para().text
assert_equal "Update", button().text

button().trigger_click
assert_equal "brand new text", para().text
