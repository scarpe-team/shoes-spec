---
# Auto-imported from scarpe/shoes-contrib/simple/simple-draw.rb
# TODO: Add meaningful assertions
----------- app code
Shoes.app do
  background "#999"
  stroke "#000"
  x, y = nil, nil
  motion do |_x, _y|
    if x and y and (x != _x or y != _y)
      append do
        line x, y, _x, _y
      end
    end
    x, y = _x, _y
  end
end

----------- test code
# Mouse drawing demo - requires motion event handling
skip "PLAYTEST: Mouse motion drawing - requires manual interaction"
