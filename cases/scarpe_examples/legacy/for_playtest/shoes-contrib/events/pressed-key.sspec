---
# Auto-imported from scarpe/shoes-contrib/events/pressed-key.rb
# TODO: Add meaningful assertions
----------- app code
Shoes.app do
  @info = para "NO KEY is PRESSED."
  keypress do |k|
    @info.replace "#{k.inspect} was PRESSED."
  end
end

----------- test code
# Verify initial state shows no key pressed
p = para()
assert p, "Expected para element to exist"
assert_equal "NO KEY is PRESSED.", p.text
# Keypress events require keyboard input - manual playtest needed
