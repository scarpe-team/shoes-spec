---
# Auto-imported from scarpe/shoes-contrib/manipulation/prepend-slot.rb
# TODO: Add meaningful assertions
----------- app code
Shoes.app do
  @slot = stack { para 'Good Morning' }
  button "Prepend" do
    @slot.prepend { para "Your car is ready." }
  end
end

----------- test code
# Verify stack and para exist
stk = stack()
assert stk, "Expected stack element to exist"

p = para()
assert p, "Expected para element to exist"
