---
# Auto-imported from scarpe/shoes-contrib/basic/scribble.rb
# TODO: Add meaningful assertions
----------- app code
Shoes.app do
  ox, oy = nil, nil
  animate 24 do
    b, x, y = self.mouse
    line(ox, oy, x, y)  if b == 1
    ox, oy = x, y
  end
end

----------- test code
# Mouse drawing app with animation loop
# No testable text content - interactive drawing demo
assert true, "Scribble drawing app runs successfully"
