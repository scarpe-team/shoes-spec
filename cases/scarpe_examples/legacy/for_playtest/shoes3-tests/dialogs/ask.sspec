---
# Auto-imported from scarpe/shoes3-tests/dialogs/ask.rb
# Tests the ask dialog with stubbing
----------- app code
Shoes.app do
  button "ask" do
    ans = ask "Feeling lucky?"
    para ans
  end
end

----------- test code
# Verify button exists
assert_equal "ask", button().text

# Stub ask() to return a known value, then trigger the button
stub_ask(returns: "Very lucky!")
button().trigger_click

# The para should now contain our stubbed response
assert_equal "Very lucky!", para().text
