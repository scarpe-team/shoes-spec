---
# Auto-imported from scarpe/shoes3-tests/radio/clear.rb
# TODO: Add meaningful assertions
----------- app code
Shoes.app do
    def main slot, stuff
        slot.clear do
            stuff.each_with_index do |x, i|
                flow do                             ####### FIX 1 : COMMENT THIS LINE 
                   r =  radio :item, checked: true; para "Radio-#{i}" 
                   r.checked = true if i == 1 
                end                                   ####### FIX 1 : COMMENT THIS LINE 
            end
        end
    end
    stack do    
      ITEMS = Array.new( 3, 1 )
      desktop = stack left: 20, top: 40, width: 200 
      main desktop, ITEMS
      button "Reset" do
          main  desktop, ITEMS
      end
    end
end

----------- test code
# Test the radio clear demo
assert_equal "Reset", button().text
assert_equal "Radio-0", para(0).text
assert_equal "Radio-1", para(1).text
assert_equal "Radio-2", para(2).text
