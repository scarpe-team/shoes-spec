---
# Auto-imported from scarpe/shoes3-tests/curl/m3.rb
# TODO: Add meaningful assertions
----------- app code
Shoes.app do
   stack do
     title "GET Google", :size => 16
     @status = para "One moment..."

     download "http://www.google.com/search?q=shoes", 
         :method => "GET" do |dump|
       @status.text = dump.response.body
     end
   end
 end

----------- test code
# Test the initial UI elements
assert_equal "GET Google", title().text
assert_equal "One moment...", para().text
