---
# Auto-imported from scarpe/shoes3-tests/curl/m1.rb
# TODO: Add meaningful assertions
----------- app code
Shoes.app do
   stack do
     title "Searching Google", :size => 16
     @status = para "One moment..."

     # Search Google for 'shoes' and print the HTTP headers
     download "http://www.google.com/search?q=shoes" do |goog|
       @status.text = "Headers: " + goog.response.headers.inspect
     end
   end
 end

----------- test code
# Test the initial UI elements
t = titles().first
assert t, "Should have a title"
assert_equal "Searching Google", t.text
p = paras().first
assert p, "Should have a para"
assert_equal "One moment...", p.text
