---
# Auto-imported from scarpe/shoes_manual/animate.rb
# TODO: Add meaningful assertions
----------- app code
#!ruby
 Shoes.app do
   @counter = para "STARTING"
   animate(24) do |frame|
     @counter.replace "FRAME #{frame}"
   end
 end

----------- test code
# Test the animation demo - initial state
# The para starts with "STARTING" then quickly becomes "FRAME X"
# We just verify the para exists and contains frame text
assert_includes para().text, "FRAME"
