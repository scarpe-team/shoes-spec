---
# Auto-imported from scarpe/shoes_manual/mouse.rb
# TODO: Add meaningful assertions
----------- app code
#!ruby
 Shoes.app do
   @p = para
   animate do
     button, left, top = self.mouse
     @p.replace "mouse: #{button}, #{left}, #{top}"
   end
 end

----------- test code
# Mouse tracking - displays button, left, top values via animate
# Requires user to move mouse to verify - VISUAL ONLY
# App code validated by running without crash
